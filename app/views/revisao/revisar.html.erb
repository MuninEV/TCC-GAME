<div class="container bg-secondary anime-left">
  <h1 class="text-center">Revisão </h1>

  <%if @array_com_dados.empty? %>
    <h1 class="text-center">Ooops!! Você não acertou nenhuma questão 😭😭😭😭 </h1>
  <%end%>

  <div class="row gap-3 p-2 flex-column">
      <% @array_com_dados.each do |a| %>
        <div class="col bg-white p-2 rounded-3 justify-center">
          <blockquote class="blockquote">
              <span class=""> <%= a[:questao] %> </span>
              <p class="lead mt-2"> <%= a[:alternativa].descricao %> </p>
              <footer class="blockquote-footer mt-2"> Explicação:  <%= a[:alternativa].explicacao %><cite title="Source Title"></cite></footer>
          </blockquote>
        </div>
      <%end%>
  </div>
</div>

<div class="row">
  <div class="col">
    <div class="d-grid gap-2 d-md-flex justify-content-md-start mt-3">
      <%= link_to "Ver pontuação", pontuacao_url(tentativa_id: params[:tentativa_id]) ,class: "btn  me-md-2 fw-bold ajusteBotao" %>
    </div>
  </div>
</div>